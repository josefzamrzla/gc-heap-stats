{
  "name": "gc-heap-stats",
  "version": "0.0.3",
  "description": "Emits detailed stats about V8 heap usage after each V8 garbage collector run",
  "main": "index.js",
  "engines": {
    "node": ">=0.12"
  },
  "scripts": {
    "test": "./node_modules/mocha/bin/mocha --expose-gc ./test.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/josefzamrzla/gc-heap-stats.git"
  },
  "keywords": [
    "gc",
    "v8",
    "heap",
    "garbage collection"
  ],
  "author": "Josef Zamrzla",
  "license": "ISC",
  "dependencies": {
    "bindings": "^1.2.1"
  },
  "devDependencies": {
    "mocha": "^3.1.2",
    "semver": "^5.3.0"
  }
}
